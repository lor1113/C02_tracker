import Transport from '../TransportForm';
import React from 'react';
import {render, fireEvent} from '@testing-library/react';

describe("Transport", ()=>{
    let container;

    beforeEach(()=>{
        container = render(<Transport/>)
    });

    it('', ()=>{

    })
})