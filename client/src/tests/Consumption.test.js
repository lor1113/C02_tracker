import Consumption from '../ConsumptionForm';
import React from 'react';
import {render, fireEvent} from '@testing-library/react';

describe("Consumption", ()=>{
    let container;

    beforeEach(()=>{
        container = render(<Consumption/>)
    });

    it('', ()=>{

    })
})