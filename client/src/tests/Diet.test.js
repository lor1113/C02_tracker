import Diet from '../DietForm';
import React from 'react';
import {render, fireEvent} from '@testing-library/react';

describe("Diet", ()=>{
    let container;

    beforeEach(()=>{
        container = render(<Diet/>)
    });

    it('', ()=>{

    })
})